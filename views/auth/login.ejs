<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | See Condo</title>
    
    
        <!-- ✅ FAVICON ADDED HERE -->
    <link rel="icon" type="image/png" href="/images/logo.png">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&family=Anton&display=swap" rel="stylesheet">
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --primary-color: #ffc107; /* Gold */
            --glass-bg: rgba(0, 0, 0, 0.6);
            --glass-border: rgba(255, 255, 255, 0.1);
        }

        body {
            font-family: 'Poppins', sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            background-color: #111;
        }

        /* --- DYNAMIC BACKGROUND --- */
        .bg-image {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            /* Use Config Image if available, else fallback to Unsplash */
            background: url('<%= (typeof config !== "undefined" && config.heroImages && config.heroImages.length > 0) ? config.heroImages[0] : "https://images.unsplash.com/photo-1542314831-068cd1dbfeeb?q=80&w=2070&auto=format&fit=crop" %>') no-repeat center center;
            background-size: cover;
            z-index: -2;
        }
        .bg-overlay {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0, 0, 0, 0.7); /* Dark tint */
            z-index: -1;
        }

        /* --- GLASS CARD --- */
        .login-card {
            width: 100%;
            max-width: 420px;
            padding: 2.5rem;
            border-radius: 20px;
            background: var(--glass-bg);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            color: white;
            animation: fadeInUp 0.8s ease-out;
        }

        /* --- INPUTS --- */
        .form-control {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: white;
            border-radius: 10px;
            padding: 12px 15px;
        }
        .form-control:focus {
            background: rgba(255, 255, 255, 0.15);
            border-color: var(--primary-color);
            box-shadow: 0 0 0 4px rgba(255, 193, 7, 0.2);
            color: white;
        }
        .form-control::placeholder { color: rgba(255, 255, 255, 0.5); }

        .input-group-text {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-right: none;
            color: var(--primary-color);
            border-radius: 10px 0 0 10px;
        }
        .form-control { border-left: none; border-radius: 0 10px 10px 0; }

        /* --- BUTTONS --- */
        .btn-gold {
            background-color: var(--primary-color);
            color: #000;
            font-weight: 700;
            border-radius: 50px;
            padding: 12px;
            border: none;
            transition: all 0.3s;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .btn-gold:hover {
            background-color: #e0a800;
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(255, 193, 7, 0.3);
        }

        .btn-google {
            background: white;
            color: #333;
            font-weight: 600;
            border-radius: 50px;
            padding: 10px;
            transition: all 0.3s;
        }
        .btn-google:hover { background: #f1f1f1; transform: translateY(-2px); }

        /* --- TEXT --- */
        h3 { font-family: 'Anton', sans-serif; letter-spacing: 2px; font-size: 2rem; }
        .divider { border-bottom: 1px solid rgba(255, 255, 255, 0.2); }
        
        @keyframes fadeInUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
    </style>
</head>
<body>

    <div class="bg-image"></div>
    <div class="bg-overlay"></div>

    <div class="container d-flex justify-content-center">
        <div class="login-card">
            
            <!-- Header -->
            <div class="text-center mb-4">
                <a href="/" class="text-decoration-none">
                    <img src="/images/logo.png" alt="Logo" style="height: 60px; width: auto; object-fit: contain; margin-bottom: 15px;">
                </a>
                <h3 class="text-white">WELCOME BACK</h3>
                <p class="text-white-50 small">Sign in to access your dashboard</p>
            </div>

            <!-- Login Form -->
            <form action="/auth/login" method="POST">
                
                <div class="mb-3">
                    <label class="small text-uppercase fw-bold text-white-50 mb-1">Username</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fas fa-user"></i></span>
                        <input type="text" name="username" class="form-control" placeholder="Enter username" required autofocus>
                    </div>
                </div>

                <div class="mb-4">
                    <label class="small text-uppercase fw-bold text-white-50 mb-1">Password</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fas fa-lock"></i></span>
                        <input type="password" name="password" id="passwordInput" class="form-control" placeholder="••••••" required>
                        <span class="input-group-text bg-transparent border-start-0" style="border-left: none; cursor: pointer; border-radius: 0 10px 10px 0;" onclick="togglePassword()">
                            <i class="fas fa-eye text-white-50" id="toggleIcon"></i>
                        </span>
                    </div>
                </div>

                <div class="d-grid mb-4">
                    <button type="submit" class="btn btn-gold shadow-sm">Login</button>
                </div>
            </form>

            <!-- Divider -->
            <div class="d-flex align-items-center mb-4">
                <div class="flex-grow-1 divider"></div>
                <span class="px-3 text-white-50 small">OR</span>
                <div class="flex-grow-1 divider"></div>
            </div>

            <!-- Google Login -->
            <div class="d-grid">
                <a href="/auth/google" class="btn btn-google shadow-sm">
                    <img src="https://www.svgrepo.com/show/475656/google-color.svg" width="20" class="me-2"> 
                    Continue with Google
                </a>
            </div>

            <div class="text-center mt-4">
                <a href="/" class="text-white-50 small text-decoration-none hover-white">
                    <i class="fas fa-arrow-left me-1"></i> Back to Website
                </a>
            </div>
        </div>
    </div>

    <script>
        function togglePassword() {
            const passwordInput = document.getElementById('passwordInput');
            const icon = document.getElementById('toggleIcon');
            
            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
            } else {
                passwordInput.type = 'password';
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
            }
        }
    </script>

</body>
</html>