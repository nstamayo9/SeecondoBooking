<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Dashboard' %> | See Condo</title>
    
    <link rel="icon" type="image/png" href="/images/logo.png">
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Anton&family=Cormorant+Garamond:wght@600&display=swap" rel="stylesheet">
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            --primary-color: #b99d75; /* Gold */
            --glass-dark: rgba(15, 23, 42, 0.6);
            --glass-border: rgba(255, 255, 255, 0.1);
            --text-light: #e2e8f0;
            --text-muted: #94a3b8;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: url('<%= (typeof config !== "undefined" && config.heroImages && config.heroImages.length > 0) ? config.heroImages[0] : "https://images.unsplash.com/photo-1542314831-068cd1dbfeeb?q=80&w=2070" %>') no-repeat center center fixed;
            background-size: cover;
            color: var(--text-light);
            min-height: 100vh;
            margin: 0;
        }

        body::before {
            content: ''; position: fixed; top: 0; left: 0; right: 0; bottom: 0;
            background: rgba(0, 0, 0, 0.6); z-index: -1;
        }

        /* --- GLASS CARDS --- */
        .glass-card {
            background: var(--glass-dark);
            backdrop-filter: blur(16px);
            border: 1px solid var(--glass-border);
            border-radius: 12px;
            transition: transform 0.3s ease;
        }
        .glass-card:hover { transform: translateY(-5px); border-color: var(--primary-color); }

        /* Typography */
        .serif-font { font-family: 'Cormorant Garamond', serif; letter-spacing: 1px; }
        .text-gold { color: var(--primary-color) !important; }
        .text-muted-glass { color: var(--text-muted) !important; }

        /* Buttons */
        .btn-gold { 
            background-color: var(--primary-color); color: #0f172a; font-weight: 600; 
            border-radius: 50px; border: none; padding: 8px 24px; transition: all 0.2s;
        }
        .btn-gold:hover { background-color: #fbbf24; color: #000; box-shadow: 0 0 15px rgba(185, 157, 117, 0.4); }

        .btn-outline-glass {
            background: transparent; border: 1px solid rgba(255,255,255,0.3); color: white; border-radius: 50px;
        }
        .btn-outline-glass:hover { background: rgba(255,255,255,0.1); color: white; border-color: white; }

        /* Inputs */
        .form-control-glass {
            background: rgba(0, 0, 0, 0.3); border: 1px solid rgba(255, 255, 255, 0.1); color: white; border-radius: 50px; padding: 10px 20px;
        }
        .form-control-glass:focus { background: rgba(0, 0, 0, 0.5); border-color: var(--primary-color); color: white; box-shadow: none; }
        .form-control-glass::placeholder { color: rgba(255,255,255,0.4); }

        .price-display { font-size: 1.5rem; font-weight: 700; color: var(--primary-color); font-family: 'Cormorant Garamond', serif; }

        /* --- PAGINATION STYLES --- */
        .pagination .page-link {
            background: rgba(0,0,0,0.5); border: 1px solid rgba(255,255,255,0.1); color: white;
            margin: 0 5px; border-radius: 50%; width: 40px; height: 40px; display: flex; align-items: center; justify-content: center;
        }
        .pagination .page-item.active .page-link {
            background: var(--primary-color); border-color: var(--primary-color); color: black; font-weight: bold;
        }
        .pagination .page-link:hover { background: rgba(255,255,255,0.1); color: var(--primary-color); }
        .pagination .page-item.disabled .page-link { background: transparent; color: #555; border-color: transparent; }
    </style>
</head>
<body>

<%- include('partials/navbar') %>

    <div class="container mt-5 mb-5 pt-5">
        
        <!-- HEADER -->
        <div class="row align-items-end mb-5">
            <div class="col-md-6">
                <h1 class="serif-font fw-bold text-white mb-0 display-4">My Bookings</h1>
                <p class="text-muted-glass">Manage your upcoming stays</p>
            </div>
            <div class="col-md-6">
                <form action="/dashboard" method="GET" class="d-flex position-relative">
                    <input type="text" name="search" class="form-control form-control-glass w-100" placeholder="Search by Room or Date..." value="<%= typeof search !== 'undefined' ? search : '' %>">
                    <button class="btn btn-gold position-absolute end-0 top-0 h-100" style="border-top-left-radius: 0; border-bottom-left-radius: 0;" type="submit"><i class="fas fa-search"></i></button>
                </form>
            </div>
        </div>

        <% if (bookings.length > 0) { %>
            <div class="row">
                <% bookings.forEach(booking => { %>
                    <div class="col-md-12 mb-4">
                        <div class="card glass-card">
                            <div class="card-body p-4">
                                <div class="row align-items-center">
                                    
                                    <!-- 1. Image -->
                                    <div class="col-lg-2 col-md-3 mb-3 mb-md-0">
                                        <% if (booking.room && booking.room.images.length > 0) { %>
                                            <img src="<%= booking.room.images[0] %>" class="rounded-3 shadow-sm w-100" style="height: 100px; object-fit: cover;">
                                        <% } else { %>
                                            <div class="bg-dark rounded-3 d-flex align-items-center justify-content-center w-100" style="height: 100px; border: 1px solid rgba(255,255,255,0.1);">
                                                <span class="text-muted small">No Image</span>
                                            </div>
                                        <% } %>
                                    </div>
                                    
                                    <!-- 2. Details -->
                                    <div class="col-lg-5 col-md-5 mb-3 mb-md-0">
                                        <div class="d-flex align-items-center mb-1">
                                            <h4 class="serif-font text-white mb-0 me-3"><%= booking.room ? booking.room.name : 'Room Unavailable' %></h4>
                                            <span class="badge rounded-pill bg-<%= booking.status === 'confirmed' ? 'success' : (booking.status === 'cancelled' ? 'danger' : (booking.status === 'completed' ? 'primary' : 'warning text-dark')) %>">
                                                <%= booking.status.toUpperCase() %>
                                            </span>
                                        </div>
                                        
                                        <div class="text-muted-glass small mb-2 font-monospace">REF: #<%= booking._id.toString().substring(0,8).toUpperCase() %></div>
                                        
                                       <div class="d-flex align-items-center mt-3">
                                            <div class="me-4 text-white"> <!-- Changed class to text-white -->
                                                <i class="fas fa-calendar-alt text-gold me-2"></i>
                                                <span class="fw-bold" style="letter-spacing: 0.5px;">
                                                    <%= new Date(booking.checkInDate).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %> 
                                                    &nbsp;&mdash;&nbsp; 
                                                    <%= new Date(booking.checkOutDate).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %>
                                                </span>
                                            </div>
                                        </div>
                                                                            </div>

                                    <!-- 3. Price -->
                                    <div class="col-lg-2 col-md-4 text-md-center border-start border-white border-opacity-10 ps-4 mb-3 mb-md-0">
                                        <div class="text-muted-glass small text-uppercase fw-bold mb-1" style="letter-spacing: 1px;">Total</div>
                                        <div class="price-display">₱<%= booking.totalPrice.toLocaleString() %></div>
                                        
                                        <% if (booking.paymentStatus === 'paid') { %>
                                            <span class="badge bg-success bg-opacity-25 text-success border border-success mt-1">PAID</span>
                                        <% } else if (booking.paymentStatus === 'partial') { %>
                                            <span class="badge bg-warning bg-opacity-25 text-warning border border-warning mt-1">PARTIAL</span>
                                        <% } else { %>
                                            <span class="badge bg-secondary bg-opacity-25 text-light border border-secondary mt-1">UNPAID</span>
                                        <% } %>
                                    </div>

                                    <!-- 4. Actions -->
                                    <div class="col-lg-3 col-md-12 text-end">
                                        <% if (booking.status === 'pending') { %>
                                            <a href="/booking/edit/<%= booking._id %>" class="btn btn-gold w-100 mb-2 shadow-sm">
                                                <i class="fas fa-credit-card me-2"></i> Pay Now
                                            </a>
                                            <form action="/booking/cancel" method="POST" onsubmit="return confirm('Cancel this booking?');">
                                                <input type="hidden" name="bookingId" value="<%= booking._id %>">
                                                <button type="submit" class="btn btn-outline-glass btn-sm w-100">Cancel</button>
                                            </form>
                                        <% } %>

                                        <% if (booking.status === 'confirmed') { %>
                                            <a href="/booking/download/<%= booking._id %>" class="btn btn-gold w-100 shadow-sm">
                                                <i class="fas fa-file-pdf me-2"></i> Download Permit
                                            </a>
                                        <% } %>

                                        <% if (booking.status === 'completed') { %>
                                            
                                            <!-- CHECK IF ALREADY RATED -->
                                            <% if (reviewedBookingIds.includes(booking._id.toString())) { %>
                                                <button class="btn btn-outline-glass w-100 mb-2 text-gold border-gold" disabled style="opacity: 0.7; cursor: not-allowed;">
                                                    <i class="fas fa-check-circle me-2"></i> Stay Rated
                                                </button>
                                            <% } else { %>
                                                <button class="btn btn-gold w-100 mb-2 shadow-sm" data-bs-toggle="modal" data-bs-target="#reviewModal-<%= booking._id %>">
                                                    <i class="fas fa-star me-2"></i> Rate Stay
                                                </button>
                                            <% } %>

                                            <a href="/booking/download/<%= booking._id %>" class="btn btn-outline-glass btn-sm w-100">View Permit</a>
                                        <% } %>

                                        <% if (booking.status === 'cancelled') { %>
                                            <a href="/room/<%= booking.room ? booking.room._id : '' %>" class="btn btn-outline-glass w-100">
                                                <i class="fas fa-redo me-2"></i> Rebook
                                            </a>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- REVIEW MODAL -->
                    <div class="modal fade" id="reviewModal-<%= booking._id %>" tabindex="-1">
                        <div class="modal-dialog modal-dialog-centered">
                            <div class="modal-content border-0 shadow-lg" style="background: #1e293b; color: white;">
                                <form action="/booking/review" method="POST">
                                    <div class="modal-header border-secondary">
                                        <h5 class="modal-title serif-font text-gold">Rate your stay</h5>
                                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                                    </div>
                                    <div class="modal-body">
                                        <input type="hidden" name="bookingId" value="<%= booking._id %>">
                                        <div class="mb-4 text-center">
                                            <label class="form-label text-white-50">How was your experience?</label>
                                            <select name="rating" class="form-select bg-dark text-warning border-warning fw-bold text-center">
                                                <option value="5">⭐⭐⭐⭐⭐ (Excellent)</option>
                                                <option value="4">⭐⭐⭐⭐ (Good)</option>
                                                <option value="3">⭐⭐⭐ (Okay)</option>
                                                <option value="2">⭐⭐ (Poor)</option>
                                                <option value="1">⭐ (Terrible)</option>
                                            </select>
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label text-white-50">Your Comments</label>
                                            <textarea name="comment" class="form-control bg-dark text-white border-secondary" rows="4" required placeholder="Tell us what you liked..."></textarea>
                                        </div>
                                    </div>
                                    <div class="modal-footer border-secondary">
                                        <button type="button" class="btn btn-outline-light rounded-pill" data-bs-dismiss="modal">Cancel</button>
                                        <button type="submit" class="btn btn-gold px-4">Submit</button>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>

            <!-- PAGINATION -->
            <% if (typeof totalPages !== 'undefined' && totalPages > 1) { %>
            <nav class="mt-5">
                <ul class="pagination justify-content-center">
                    
                    <!-- Prev -->
                    <li class="page-item <%= currentPage == 1 ? 'disabled' : '' %>">
                        <a class="page-link" href="/dashboard?page=<%= currentPage - 1 %>&search=<%= typeof search !== 'undefined' ? search : '' %>">
                            <i class="fas fa-chevron-left"></i>
                        </a>
                    </li>

                    <!-- Numbers -->
                    <% for(let i=1; i<=totalPages; i++) { %>
                        <li class="page-item <%= currentPage == i ? 'active' : '' %>">
                            <a class="page-link" href="/dashboard?page=<%= i %>&search=<%= typeof search !== 'undefined' ? search : '' %>"><%= i %></a>
                        </li>
                    <% } %>

                    <!-- Next -->
                    <li class="page-item <%= currentPage == totalPages ? 'disabled' : '' %>">
                        <a class="page-link" href="/dashboard?page=<%= currentPage + 1 %>&search=<%= typeof search !== 'undefined' ? search : '' %>">
                            <i class="fas fa-chevron-right"></i>
                        </a>
                    </li>

                </ul>
            </nav>
            <% } %>

        <% } else { %>
            <div class="glass-card text-center py-5">
                <i class="fas fa-calendar-times fa-4x mb-4 text-gold opacity-50"></i>
                <h2 class="serif-font text-white">No bookings found</h2>
                <p class="text-white-50">You haven't made any reservations yet.</p>
                <a href="/" class="btn btn-gold px-5 mt-3">Browse Rooms</a>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>